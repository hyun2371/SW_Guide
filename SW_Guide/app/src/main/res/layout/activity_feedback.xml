<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <Button
        android:id="@+id/feed_back_btn"
        android:layout_width="22dp"
        android:layout_height="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@drawable/ic_back_btn"
        android:layout_marginTop="30dp"
        android:layout_marginStart="11dp"
        />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="피드백 보내기"
        app:layout_constraintStart_toEndOf="@id/feed_back_btn"
        android:layout_marginStart="5dp"
        app:layout_constraintTop_toTopOf="@id/feed_back_btn"
        app:layout_constraintBottom_toBottomOf="@id/feed_back_btn"
        android:textSize="18sp"
        android:textStyle="bold"/>

    <EditText
        android:id="@+id/edit1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_marginStart="30dp"
        android:layout_marginEnd="30dp"
        android:hint="피드백 내용"
        app:layout_constraintTop_toBottomOf="@id/feed_back_btn"
        app:layout_constraintStart_toStartOf="parent"/>
    <Button
        android:id="@+id/feedback_send_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:layout_marginStart="40dp"
        android:layout_marginEnd="40dp"
        app:layout_constraintTop_toBottomOf="@id/edit1"
        app:layout_constraintStart_toStartOf="parent"
        android:textSize="16sp"
        android:text="이메일로 피드백 전송"/>

</androidx.constraintlayout.widget.ConstraintLayout>